SUBDIRS = src doc scripts tests

maintainer-clean-local:
	rm aclocal.m4 Makefile.in config.h.in configure depcomp install-sh missing
	-rm compile
	-rm config.h-in~ config.sub
	-find . -name '*.log' -delete
	-rm -rf oprofile_data
